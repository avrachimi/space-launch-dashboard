<!DOCTYPE html>
<html lang="en">

<head>
    <title>Space Launch Dashboard</title>
    <link rel="stylesheet" href="/css/iss.css">
    <link rel="stylesheet" href="/css/styles.css">
    <meta charset="utf-8">
    <link rel="shortcut icon" href="#">
</head>

<body>
    <header>
        <h3>Space Launch Dashboard</h3>
        <nav>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/launches" class="current">Launches</a></li>
                <li><a href="/iss">ISS</a></li>
                <li><a href="/starship">Starship</a></li>
            </ul>
        </nav>
        <a class="callToAction" target="_blank" href="https://github.com/avrachimi/space-launch-dashboard"><button>Github</button></a>
    </header>
    <div class="tables">
        <h2>Astronauts Currently Living on the International Space Station</h2>
        <table>
            <tr>
                <th>Name</th>
                <th>Role</th>
                <th>Agency</th>
                <th>Photo</th>
            </tr>
            <% for (var i=0; i < locals.crew.length; i++) { %>
                <tr>
                    <td><%= locals.crew[i].name %></td>
                    <td><%= locals.crew[i].role %></td>
                    <td><%= locals.crew[i].agency %></td>
                    <td><a target="_blank" href=<%= locals.crew[i].img_url %>>view</a></td>
                </tr>
            <% } %>
        </table>
        <br/>
        <h2>Docked Vehicles</h2>
        <table>
            <tr>
                <th>Name</th>
                <th>Docked At*</th>
            </tr>
            <% for (var i=0; i < locals.docked_vehicles.length; i++) { %>
                <% if (locals.docked_vehicles[i].docked_at) { %> 
                <tr>
                    <td><%= locals.docked_vehicles[i].name %></td>
                    <td><%= new Date(locals.docked_vehicles[i].docked_at).toUTCString().slice(0, 26) %></td>
                </tr>
                <% } %>
            <% } %>
        </table>
        <p><strong>* All times are in UTC</strong></p>
    </div>
    <script src="/js/iss.js"></script>
</body>

</html>